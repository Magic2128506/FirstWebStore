@model IEnumerable<Employee>

@{
    ViewData["Title"] = "Сотрудники";
}

<div class="container">
    <h3>@ViewData["Title"]</h3>

    <hr />
    <table class="table table-bordered" id="employees-table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Фамилия</th>
                <th>Имя</th>
                <th>Отчество</th>
                <th>Возраст</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var employee in Model)
            {
                <tr>
                    <td>@employee.ID</td>
                    <td>@employee.FirstName</td>
                    <td>@employee.SurName</td>
                    <td>@employee.Patronymic</td>
                    <td>@employee.Age</td>
                    <td>
                        <a asp-controller="Employees" asp-action="Details" asp-route-id="@employee.ID">Открыть</a>
                    </td>
                    <td>
                        <a asp-controller="Employees" asp-action="Edit" asp-route-id="@employee.ID">Внести изменения</a>
                    </td>
                </tr>
            }
        <a asp-controller="Employees" asp-action="Edit">Добавить сотрудника</a>
        </tbody>
    </table>
</div>

@section Styles {
    <style>
        #employees-table {
            background: #F0F0E9;
        }
    </style>
}

@section Scripts {
    <script>
        myscript
    </script>
}